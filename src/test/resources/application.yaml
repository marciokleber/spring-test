spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/test?useSSL=false&allowPublicKeyRetrieval=true}
    username: ${SPRING_DATASOURCE_URL:root}
    password: ${SPRING_DATASOURCE_URL:root}
    driver-class-name: com.mysql.cj.jdbc.Driver